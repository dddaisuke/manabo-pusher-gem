require 'spec_helper'

describe Manabo::Pusher do
  it 'has a version number' do
    expect(Manabo::Pusher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
